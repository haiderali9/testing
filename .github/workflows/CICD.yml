name: Production Build
on: 
  push:
   branches:
      - master
jobs:
  build:
    runs-on: node-latest
    
    strategy:
      matrix:
        node-version: [14.x]
        
    steps:
    - uses: actions/checkout@v1
    - run: npm install 
    - run: npm run build:dev
